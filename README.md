# Luvit for Mavericks (OS X 10.9)

shared contains binaries for people with luv-luajit installed.

bundled contains binaries and libraries with import part modified, and an import path modifier script that is horrendous but works.
